interface:
  display_name: "SDD Apply"
  short_description: "Apply SDD changes with available guidance"
  default_prompt: "Use $sdd-apply to implement the active SDD change with relevant available skills, delegated Requirement slices, verification, and durable Epic reconciliation."
