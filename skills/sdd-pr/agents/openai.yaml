interface:
  display_name: "SDD PR"
  short_description: "Open and steward SDD pull requests"
  default_prompt: "Use $sdd-pr to steward an SDD-backed pull request through the configured provider, reconcile accepted feedback with Epic truth and review freshness, and stop before merge until the user approves."
