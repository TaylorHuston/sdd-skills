interface:
  display_name: "SDD Release"
  short_description: "Prepare SDD release handoffs"
  default_prompt: "Use $sdd-release to review the changelog, confirm version intent, run release gates, push the resolved source branch, and open the configured release PR."
