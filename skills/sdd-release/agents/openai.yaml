interface:
  display_name: "SDD Release"
  short_description: "Prepare SDD release handoffs"
  default_prompt: "Use $sdd-release to run the project-defined release checks, update required release communication, and prepare the configured production handoff."
